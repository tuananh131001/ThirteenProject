<h1><%= @blog.title %></h1>

<p><%= @blog.content %></p>

<ul>
  <li><%= link_to "Edit", edit_blog_path(@blog) %></li>
  <li><%= link_to "Destroy", blog_path(@blog), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></li>
</ul>
