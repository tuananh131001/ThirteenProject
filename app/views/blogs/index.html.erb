<section class="flex flex-col">
  <h1 class="mb-4 text-4xl font-extrabold leading-none tracking-tight text-gray-900 md:text-5xl lg:text-6xl dark:text-white">Blogs</h1>
  <br/>
  <div class="gap-4 join join-vertical lg:join-horizontal">
    <% @blogs.each do |blog| %>
        <%= render(Blogs::ArticleComponent.new(article: blog))%>
   <% end %> 
  </div>
  <%= link_to "New Blog", new_blog_path %>
</section>
